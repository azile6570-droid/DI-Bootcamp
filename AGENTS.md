# DI-Bootcamp Agent Instructions

## Workspace Shape

- This workspace is organized as weekly exercise folders containing day folders.
- Folder names are inconsistent in capitalization (`Week`/`week`, `Day`/`day`) and include the existing `weeek 12` spelling. Preserve existing names and paths.
- The only discovered exercise file is `Week 1/Day 1/Exercise.xp`; treat `.xp` files as exercise content and preserve their format.

## Working Conventions

- Start by locating the exact week/day exercise requested. Do not infer a missing exercise from neighboring empty folders.
- Keep editor tabs visible when working with multiple exercise files so the active file and open-file context remain clear.
- Prefer workspace-relative paths in explanations and avoid broad reorganizations of the weekly folders.
- There is currently no README, package manifest, test configuration, or build configuration. Do not invent build or test commands; validate changes with the narrowest applicable check.